{
	"name": "poll",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"//": [
		"The app is Go - see the Makefile. Node/bun stays for exactly two jobs:",
		"1. Paraglide compiles messages/{da,en,fr}.json into the typed m.*() module the",
		"   e2e specs import - the same JSON the Go server renders from.",
		"2. Playwright runs the e2e suite.",
		"htmx.org + alpinejs are vendored into static/ by `make vendor`; tailwindcss",
		"builds static/app.css via `make css`."
	],
	"scripts": {
		"messages": "paraglide-js compile --project ./project.inlang --outdir ./src/lib/paraglide",
		"test:e2e": "playwright test",
		"format": "prettier --write .",
		"format:check": "prettier --check .",
		"prepare": "bun run messages || echo '' ; git config core.hooksPath .githooks"
	},
	"dependencies": {
		"alpinejs": "^3.15.12",
		"htmx.org": "^2.0.10"
	},
	"devDependencies": {
		"@inlang/paraglide-js": "^2.20.2",
		"@playwright/test": "^1.61.1",
		"@tailwindcss/cli": "^4.3.0",
		"@types/node": "^26.0.1",
		"prettier": "^3.9.4",
		"tailwindcss": "^4.3.0",
		"typescript": "^6.0.3"
	}
}
